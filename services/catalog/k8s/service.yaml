apiVersion: v1
kind: Service
metadata:
  name: s-catalog
  namespace: stage
spec:
  type: NodePort
  ports:
  - name: main
    port: 8083
    protocol: TCP
    targetPort: http
  selector:
    app: s-catalog
